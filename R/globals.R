globalVariables(unique(c(
  # D_index:
  "dn", "sex", "z",
  # Hedges_g:
  "g", "lower",
  # MI_index:
  "dn", "sex", "z",
  # Van_vark_raw:
  "Trait",
  # add_sig:
  "p.value",
  # van_vark:
  "F.mu", "M", "M.mu", "no", "x1", "x2"
)))
